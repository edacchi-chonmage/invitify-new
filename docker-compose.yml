services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=invitify-database'
      - 'MYSQL_PASSWORD=root'
      - 'MYSQL_ROOT_PASSWORD=root'
      - 'MYSQL_USER=myuser'
    ports:
      - '3306'
